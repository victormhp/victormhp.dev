<a class="resume" href="/resume.pdf" target="_blank" rel="noopener noreferrer">
  <slot />
</a>

<style>
  a {
    border-radius: 0.25rem;
    border: 2px solid var(--color-gray-800);
    padding: 0.5rem 1rem;
    background-color: var(--color-light);
    color: var(--color-gray-800);
    box-shadow:
      0.5px 1px 0 0 var(--color-gray-800),
      1.5px 2px 0 0 var(--color-gray-800),
      2.5px 3px 0 0 var(--color-gray-800),
      3.5px 4px 0 0 var(--color-gray-800),
      4.5px 5px 0 0 var(--color-gray-800),
      5.5px 6px 0 0 var(--color-gray-800);
    position: relative;
    top: 0;
    left: 0;
    transform: rotateX(0) rotateY(0) rotateZ(0);
    transition: all 0.5s;
    cursor: pointer;
    display: inline-block;
    outline: 2px dashed transparent;
    position: relative;

    &:hover {
      box-shadow:
        0 0 0 0 var(--color-black-200),
        0 0 0 0 var(--color-black-200),
        0 0 0 0 var(--color-black-200),
        0 0 0 0 var(--color-black-200),
        0 0 0 0 var(--color-black-200),
        0 0 0 0 var(--color-black-200);
      top: 6px;
      left: 5.5px;
      transform: rotateX(5deg) rotateY(3deg) rotateZ(0.25deg);
    }

    &:focus-visible {
      --background: var(--color-gray-50);

      box-shadow:
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 0 var(--color-gray-800),
        0 0 0 2px var(--background),
        0 0 0 4px var(--color-gray-800);
      outline: none;
    }
  }
</style>
